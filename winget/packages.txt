# Windows-side packages installed via winget.
# One entry per line: `<package-id> [source]`. Comments (#) and blank lines ignored.
# Source defaults to winget. Use `msstore` for Microsoft Store-only packages.
# Find IDs with `winget search <name>`.

# Tiling window manager (installer bundles Zebar)
glzr-io.glazewm

# 9PFXXSHC64H3 msstore
Flow-Launcher.Flow-Launcher

# Keyboard scripting — required by winget/winkey.ahk
AutoHotkey.AutoHotkey

# Process/system monitor — Windows-side btm, run from WSL as btm.exe (btmw alias)
# to see Windows processes without opening Task Manager
Clement.bottom

# Screenshot capture tool
ShareX.ShareX
