# Mac OS X
.AppleDouble
.DS_Store
.LSOverride
._*

# Windoze
$RECYCLE.BIN/
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# (Neo)Vim
*~
.netrwhist
Session.vim
Sessionx.vim
[._]*.s[a-v][a-z]
[._]*.sw[a-p]
[._]s[a-rt-v][a-z]
[._]ss[a-gi-z]
[._]sw[a-p]
[._]*.un~
tags

# VS Code
.history
.vscode

# JetBrains
.idea

# Superpowers
docs/superpowers/

# tacky-borders rotating-state config (seeded from themes/violet-pink.yaml at
# install, written by rotate.ps1 / tacky-theme). See install.linux.sh.
tacky-borders/config.yaml
# tacky-borders writes its runtime log into TACKY_BORDERS_CONFIG_HOME.
tacky-borders/tacky-borders.log

# Zebar runtime artifacts — Zebar reads its config directly from this dir via
# `--config-dir` (see install.linux.sh / reload-zebar.ps1), and writes its
# error log + marketplace cache alongside the pack.
zebar/errors.log
zebar/.marketplace/
